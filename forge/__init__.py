"""schema-forge — CLI schema generator.

Produces the five artifacts the spine consumes (schema, modifiers, policy,
failure modes, gold corpus). Files-only contract: this package never imports
the spine, and the spine never imports this package. See docs/INTERFACE.md.
"""

__version__ = "0.0.0"
